{
  "private": true,
  "scripts": {
    "dev": "parcel src/index.html --open",
    "build": "rm -f build/*; parcel build src/index.html --dist-dir build"
  },
  "devDependencies": {
    "@types/p5": "^1.4.3",
    "parcel": "^2.8.3"
  },
  "dependencies": {
    "p5": "^1.5.0"
  }
}
